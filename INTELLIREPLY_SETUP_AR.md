# 🛠️ دليل الإعداد والتفعيل الكامل — IntelliReply (Smart Router AI)

> **لمن؟** لصاحب الـ Platform (SaaS Owner) وللمشتركين في الخدمة  
> **المسار:** `/automation/ai`  
> **المشروع:** Botzo

---

## 📖 فهرس الدليل

1. [للـ Platform Admin: إعداد المنصة أولاً](#1-للـ-platform-admin-إعداد-المنصة-أولاً)
2. [للمشترك: تفعيل AI في حسابك](#2-للمشترك-تفعيل-ai-في-حسابك)
3. [رفع قاعدة المعرفة (Knowledge Base)](#3-رفع-قاعدة-المعرفة-knowledge-base)
4. [إعداد الردود التلقائية](#4-إعداد-الردود-التلقائية)
5. [إدارة الكلمات المفتاحية](#5-إدارة-الكلمات-المفتاحية)
6. [من يدفع تكلفة OpenAI؟](#6-من-يدفع-تكلفة-openai)
7. [إعداد حدود الاستخدام والـ Billing](#7-إعداد-حدود-الاستخدام-والـ-billing)
8. [دعم الصوت](#8-دعم-الصوت)
9. [متغيرات البيئة (.env)](#9-متغيرات-البيئة-env)
10. [استكشاف الأخطاء](#10-استكشاف-الأخطاء)

---

## 1. للـ Platform Admin: إعداد المنصة أولاً

### الخطوة 1: تفعيل الـ Module في لوحة الأدمن

اذهب إلى: `/admin/addons` أو `/admin/features`

ابحث عن: **AI Assistant** ثم فعّله.

---

### الخطوة 2: اختيار سياسة المفتاح (Key Policy)

**الخيارات المتاحة:**

#### الخيار A: `hybrid` (الافتراضي — الأكثر مرونة)
```
- كل مشترك ممكن يستخدم مفتاحه الخاص
- أو يستخدم المفتاح العالمي للـ Platform
- أنت بتتحكم في مين يقدر يستخدم المفتاح العالمي
```

#### الخيار B: `global_only` (Platform يتحكم الكل)
```
- كل المشتركين بيستخدموا مفتاح الـ Platform فقط
- مفيش مشترك يقدر يدخل مفتاحه الخاص
- ⚠️ هتحتاج تدخل مفتاح عالمي إجباري!
```

#### الخيار C: `organization_only` (كل مشترك مسؤول عن نفسه)
```
- كل مشترك لازم يدخل مفتاحه الخاص
- مفيش مفتاح عالمي
- مفيش تكلفة على الـ Platform
```

---

### الخطوة 3: إدخال المفتاح العالمي (لو اخترت global أو hybrid)

في لوحة الأدمن > AI Assistant Settings:

```
Global OpenAI API Key: sk-proj-xxxxxxxxxxxx
```

**مهم جداً:**
- المفتاح بيتخزن مشفّر في قاعدة البيانات
- مش بيظهر تاني بعد الحفظ
- لو نسيته، دخّل واحد جديد

---

### الخطوة 4: ضبط Allow Organization Override

**لو السياسة `hybrid`:**
- **مفعّل (افتراضي):** المشترك يقدر يستخدم مفتاحه بدل المفتاح العالمي
- **موقف:** كل المشتركين هيستخدموا المفتاح العالمي فقط حتى في الـ hybrid

---

### الخطوة 5: إعداد حدود الاستخدام في الباقات

في `/admin/subscription-plans` > تعديل الباقة:

```json
{
  "ai_text_response_limit": 500,    // عدد الردود النصية في الشهر (-1 = بلا حد)
  "ai_audio_response_limit": 100,   // عدد الردود الصوتية في الشهر (-1 = بلا حد)
  "ai_organization_key_enabled": true, // هل يقدر المشترك يستخدم مفتاحه؟
  "ai_system_key_monthly_quota": 200 // حد استخدامه من المفتاح العالمي في الشهر
}
```

---

### الخطوة 6: تفعيل AI Billing (لو عايز تطبق الحدود)

في إعدادات النظام:
```
enable_ai_billing = 1
```

**لو = 0:** مفيش حدود مطبّقة بغض النظر عن الباقة.

---

## 2. للمشترك: تفعيل AI في حسابك

### اذهب إلى: `/automation/ai`

### الخطوة 1: ضبط المفتاح والنموذج

اضغط **Update** وستظهر نافذة الإعدادات:

| الإعداد | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| Key Source | مصدر المفتاح (auto/global/organization) | auto |
| OpenAI API Key | مفتاحك الخاص من OpenAI (لو مسموح) | - |
| Model | نموذج ChatGPT اللي هيستخدمه | gpt-4o-mini |
| Embedding Model | نموذج تحويل النص لـ vectors | text-embedding-3-small |
| Max Tokens | الحد الأقصى لطول الرد | 512 |
| Temperature | درجة إبداعية الرد (0=دقيق، 2=إبداعي) | 0.7 |

### الخطوة 2: اختيار النموذج المناسب

**للشركات العادية (دعم عملاء):**
```
Model: gpt-4o-mini ← الأسرع والأرخص
أو: gpt-4.1-mini ← أحدث وأذكى قليلاً
```

**للمحتوى المعقد (تقنية، طبي...):**
```
Model: gpt-4o ← أذكى وأغلى
أو: gpt-4.1 ← الأحدث
```

**للردود الصوتية:**
```
Model: gpt-audio-1.5 ← مخصص للصوت
```

### الخطوة 3: حفظ الإعدادات والتفعيل

1. ادخل إعداداتك
2. فعّل زر **Enable AI Assistant** (التوغل Switch)
3. النظام هيتحقق من المفتاح بشكل تلقائي عن طريق Embedding Preflight
4. لو المفتاح صح → AI مفعّل ✅

---

## 3. رفع قاعدة المعرفة (Knowledge Base)

### أنواع الملفات المدعومة:
| النوع | الامتداد | الأفضل لـ |
|-------|---------|----------|
| PDF | .pdf | كتالوجات، سياسات، وثائق رسمية |
| Word | .docx, .doc | مستندات قابلة للتعديل |
| نص عادي | .txt | FAQ، معلومات بسيطة |

### كيف تكتب مستند فعّال؟

**✅ صح:**
```
# سياسة الإرجاع

يمكن إرجاع المنتجات خلال 14 يوماً من تاريخ الاستلام.
المنتج يجب أن يكون بحالته الأصلية مع جميع ملحقاته.
الفاتورة مطلوبة للإرجاع.

لتقديم طلب إرجاع، اتصل بنا على: 0100-xxx-xxxx
أو أرسل إيميل على: returns@company.com
```

**❌ غلط:**
```
أسعارنا حلوة وعندنا كل حاجة
للتواصل معنا على الإيميل
```

### نصائح للمستندات:
1. **محتوى واضح ومنظم** — الـ AI بيقرأه كما هو
2. **ملفات منفصلة** — ملف للأسعار، ملف للسياسات، ملف للمنتجات
3. **أسئلة وأجوبة** — أفضل طريقة للمعلومات
4. **العربية والإنجليزية** — كلاهما مدعومان
5. **تجنب الصور والجداول المعقدة** — PDF بيعمل extract للنص فقط

### خطوات الرفع:
1. اضغط **Upload Documents**
2. اختر الملف + اكتب عنوان وصفي
3. انتظر حتى يظهر Status = Complete ✅
4. الـ AI جاهز يستخدم المعلومات دي!

---

## 4. إعداد الردود التلقائية

في قسم **AI Assistant Setup** في صفحة `/automation/ai`:

### خيار 1: الردود التلقائية للزبائن الجدد

```
Enable automatic AI assistance for new conversations: ✓ مفعّل
```

**ماذا يعني ذلك؟**
- **مع نظام التذاكر (Ticketing):** لو الزبون فتح محادثة ومفيش رد من الفريق من آخر فتح تذكرة → AI يرد تلقائياً
- **بدون نظام التذاكر:** لو الزبون بعت ≤1 رسالة في آخر 24 ساعة → AI يرد تلقائياً

### خيار 2: AI للزبائن الفرديين

في صفحة المحادثة مع أي زبون:
- يوجد زر تفعيل AI خاص بهذا الزبون
- أو في `/automation/contact/{uuid}` عبر API

---

## 5. إدارة الكلمات المفتاحية

### كلمات التشغيل (Start Keywords):
```
مثال: مرحبا, hi, hello, ابدأ, start, bot, بوت
```
**ماذا تفعل:** لو الزبون كتب أي كلمة من دول → AI يشتغل لهذا الزبون

### كلمات الإيقاف (Stop Keywords):
```
مثال: agent, وكيل, انسان, human, stop, موقف, مسؤول
```
**ماذا تفعل:** لو الزبون كتب أي كلمة من دول → AI يوقف لهذا الزبون + الموظف يتولى

### نصائح:
- افصل بين الكلمات بفاصلة `,`
- ممكن تستخدم عربي وإنجليزي في نفس الوقت
- المقارنة مش case-sensitive (كبير/صغير سواء)
- الكلمة ممكن تكون جزء من جملة (مش لازم كلمة لوحدها)

---

## 6. من يدفع تكلفة OpenAI؟

### جدول توضيحي:

| السياسة | Key Source | من يتحاسب مع OpenAI |
|---------|-----------|---------------------|
| `global_only` | Platform key | Platform Owner دايماً |
| `organization_only` | Org key | كل مشترك بنفسه |
| `hybrid` + org key | Organization | المشترك بنفسه |
| `hybrid` + global key | Platform | Platform Owner |

### تقدير التكلفة:

**لكل رسالة زبون (تقريباً):**

| المرحلة | النموذج | التكلفة التقريبية |
|---------|--------|----------------|
| Embedding السؤال | text-embedding-3-small | ~$0.00002 |
| توليد الرد | gpt-4o-mini (512 token) | ~$0.0003 |
| **المجموع** | | **~$0.0003 لكل رسالة** |

يعني 1000 رسالة ≈ $0.30 (30 سنت)

**للصوت:**
| المرحلة | التكلفة التقريبية |
|---------|----------------|
| Transcription (60 ثانية) | ~$0.006 |
| Audio Response | أغلى من النص بـ 10x |

---

## 7. إعداد حدود الاستخدام والـ Billing

### السيناريو 1: Platform يدفع، بيحدد حدود للمشتركين

```
Admin Settings:
  enable_ai_billing = 1
  ai_key_policy = global_only
  ai_global_api_key = sk-proj-...

Subscription Plan (مثلاً باقة Pro):
  ai_text_response_limit = 1000     ← 1000 رد نصي في الشهر
  ai_audio_response_limit = 200     ← 200 رد صوتي في الشهر
  ai_system_key_monthly_quota = 500 ← 500 رسالة max من مفتاحه العالمي
```

### السيناريو 2: كل مشترك يدفع بنفسه

```
Admin Settings:
  enable_ai_billing = 0  (أو organization_only policy)
  ai_key_policy = organization_only

المشترك:
  يدخل مفتاحه الخاص
  يتحاسب مباشرة مع OpenAI
  مفيش حدود من الـ Platform
```

### السيناريو 3: مرن (Hybrid)

```
Admin Settings:
  ai_key_policy = hybrid
  ai_allow_org_override = 1
  ai_global_api_key = sk-proj-...

الأولوية:
  لو المشترك عنده مفتاح خاص → يُستخدم (مفيش حد من Platform)
  لو مفيش → يستخدم المفتاح العالمي (بتطبق حدود الباقة)
```

---

## 8. دعم الصوت

### لتفعيل الردود الصوتية:

1. اختار نموذج صوتي من الـ Model list:
   - `gpt-audio-1.5`
   - `gpt-audio`
   - `gpt-audio-mini`
   - `gpt-4o-audio-preview`

2. فعّل **Activate audio responses** ✓

3. اختار **Voice** (الصوت):
   - alloy, echo, fable, onyx, nova, shimmer (أصوات OpenAI)

**ملاحظة:** الصوت الوارد من الزبون بيتحوّل لنص أولاً (Transcription) ثم يتم الرد.

**متطلبات للصوت:**
- FFmpeg مثبّت على السيرفر (للتحويل بين صيغ الصوت)
- حد الاستخدام الصوتي أغلى من النصي

---

## 9. متغيرات البيئة (.env)

```env
# ————— IntelliReply Embedding Models —————
INTELLIREPLY_DEFAULT_EMBEDDING_MODEL=text-embedding-3-small
INTELLIREPLY_EMBEDDING_MODELS=text-embedding-3-small,text-embedding-3-large

# ————— Audio Models —————
INTELLIREPLY_AUDIO_RESPONSE_MODELS=gpt-audio-1.5,gpt-audio,gpt-audio-mini,gpt-4o-audio-preview

# ————— Transcription Models —————
INTELLIREPLY_DEFAULT_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
INTELLIREPLY_TRANSCRIPTION_MODEL_FALLBACKS=gpt-4o-mini-transcribe,whisper-1

# ————— FFmpeg (لو محتاج Audio) —————
FFMPEG_BINARIES=/usr/bin/ffmpeg
FFPROBE_BINARIES=/usr/bin/ffprobe
```

---

## 10. استكشاف الأخطاء

### المشكلة: AI مفعّل بس مش بيرد

**افحص الترتيب:**
1. هل `AI Assistant` module مفعّل للمنظمة؟
2. هل `ai.active = true` في metadata المنظمة؟
3. هل `ai_assistance_enabled = 1` للجهة اللي بتبعت؟
4. هل مفيش stop keyword في الرسالة؟
5. هل في مستند مرفوع في قاعدة المعرفة؟
6. هل المفتاح صح؟

**افحص اللوج:**
```bash
grep "intellireply" storage/logs/laravel.log | tail -50
```

---

### المشكلة: خطأ "AI API key is missing"

**الحل:**
- في `/automation/ai` > اضغط Update
- ادخل مفتاح OpenAI صحيح يبدأ بـ `sk-`
- أو تواصل مع الأدمن عشان يضيف مفتاح عالمي

---

### المشكلة: خطأ "Selected embedding model is not available"

**الحل:**
- المفتاح ده مش عنده صلاحية للـ model ده
- غيّر الـ Embedding Model لـ `text-embedding-3-small`
- أو استخدم مفتاح برنامج أعلى (paid tier)

---

### المشكلة: "AI response limit reached"

**الحل:**
- مشتركك وصل حده الشهري
- ارفع الباقة أو فعّل مفتاح المنظمة الخاص بيه
- أو انتظر بداية الشهر الجديد

---

### المشكلة: الرد بطيء أو timeout

**الحل:**
- جرب نموذج أخف: `gpt-4o-mini` بدل `gpt-4o`
- قلّل الـ Max Tokens
- قلّل عدد المستندات أو حجمها
- تحقق من سرعة الاتصال بالسيرفر

---

### المشكلة: الرد مش صلة بالسؤال

**الحل:**
- حسّن محتوى مستنداتك
- استخدم FAQ format (سؤال وجواب)
- قسّم المعلومات لملفات منفصلة واضحة
- جرب نموذج أذكى (gpt-4o بدل gpt-4o-mini)
- قلّل الـ Temperature (0.3-0.5 لأجوبة أكثر دقة)

---

## 📋 قائمة مراجعة سريعة قبل الإطلاق

```
□ Module "AI Assistant" مفعّل في لوحة الأدمن
□ سياسة المفتاح محددة (hybrid/global_only/organization_only)
□ مفتاح OpenAI مضبوط (عالمي أو للمنظمة)
□ حدود الاستخدام محددة في الباقة
□ enable_ai_billing مضبوط
□ مستند واحد على الأقل مرفوع وstatus = Complete
□ AI مفعّل للمنظمة (active = true)
□ اختبرت بإرسال رسالة تجريبية
□ كلمات التشغيل/الإيقاف محددة
□ اخترت ترتيب الاستجابة (sequence) المناسب
```

---

*للدعم التقني: راجع `INTELLIREPLY_TECHNICAL_AR.md`*  
*لتقرير الجودة: راجع `INTELLIREPLY_QA_REPORT_AR.md`*  
*للشرح التجاري: راجع `INTELLIREPLY_BUSINESS_AR.md`*
